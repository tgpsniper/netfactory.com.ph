# Live credentials — never commit
.env
api/.env
*.pem
*.key

# Customer personal data. Do not remove this line for a public repo:
# these are subscriber documents and survey files belonging to people who
# are not party to the decision to publish this repository.
web/uploads/
uploads/

# Reinstallable / runtime
node_modules/
api/node_modules/
*.log
logs/
